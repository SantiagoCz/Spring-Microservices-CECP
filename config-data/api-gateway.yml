server:
  port: 8765

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        # Auth Service
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/auth/**

        # Affiliates Service
        - id: affiliate-service
          uri: lb://affiliate-service
          predicates:
            - Path=/affiliate-service/**
          filters:
            - StripPrefix=1

        # Medical Coverage Service
        - id: medical-coverage-service
          uri: lb://medical-coverage-service
          predicates:
            - Path=/medical-coverage-service/**
          filters:
            - StripPrefix=1

        # Appointments Service
        - id: appointment-service
          uri: lb://appointment-service
          predicates:
            - Path=/appointment-service/**
          filters:
            - StripPrefix=1

        # Dental Service
        - id: dental-service
          uri: lb://dental-service
          predicates:
            - Path=/dental-service/**
          filters:
            - StripPrefix=1

        # Employee Service
        - id: employee-service
          uri: lb://employee-service
          predicates:
            - Path=/employee-service/**
          filters:
            - StripPrefix=1

      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

jwt:
  secret: A1b2C3d4E5f6G7h8I9j0K1l2M3n4O5p6Q7r8S9t0U1v2W3x4Y5z6A7b8C9d0E1f2G3==

internal:
  secret: cecp-microservices-2024-secret-key